# v0.1.0 - Refactor Tools to Reusable Modules

Extract the tools from `srs_analyst.py` into a reusable `tools/` module structure, enabling these tools to be shared across multiple agents.

## Proposed Changes

### Tools Module Structure

Create a new `src/tools/` directory with the following structure:
```
src/tools/
├── __init__.py           # Re-export all tools for easy imports
├── knowledge_base.py     # Hierarchical RAG knowledge base tool
└── redmine.py            # Redmine-related tools (issue status, wiki update)
```

---

### [NEW] [\_\_init\_\_.py](file:///Volumes/Samsung970EVOPlus/dev-projects/apollo-1/src/tools/__init__.py)

Re-exports all tools for convenient imports:
- `search_knowledge_base` function and `Search_Knowledge_Base` LangChain Tool
- `get_issue_status` function and `Get_Realtime_Issue_Status` LangChain Tool  
- `update_srs_wiki` function and `Update_SRS_Wiki` LangChain StructuredTool

---

### [NEW] [knowledge_base.py](file:///Volumes/Samsung970EVOPlus/dev-projects/apollo-1/src/tools/knowledge_base.py)

Extracts hierarchical RAG components:
- `embeddings`, `vector_db`, `store` configuration
- `hierarchical_retriever` setup with parent/child splitters
- `search_knowledge_base()` function
- `Search_Knowledge_Base` LangChain Tool wrapper

---

### [NEW] [redmine.py](file:///Volumes/Samsung970EVOPlus/dev-projects/apollo-1/src/tools/redmine.py)

Extracts Redmine-related tools:
- Redmine client initialization
- `get_issue_status()` function with `Get_Realtime_Issue_Status` Tool
- `update_srs_wiki()` function with `Update_SRS_Wiki` StructuredTool

---

### [MODIFY] [srs_analyst.py](file:///Volumes/Samsung970EVOPlus/dev-projects/apollo-1/src/agents/srs_analyst.py)

Simplify by importing tools from the new module:
- Remove tool function definitions and configurations
- Import `tools` list from `src.tools`
- Keep only agent-specific logic (prompt, agent creation, workflow)

## Verification Plan

### Automated Tests
- Run `python -m py_compile` on all new files to verify syntax

### Manual Verification
1. Run `python -c "from src.tools import all_tools"` to verify imports work
2. Run `python -c "from src.agents.srs_analyst import run_srs_to_swe2_workflow"` to verify the agent still works
